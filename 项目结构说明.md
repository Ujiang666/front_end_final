# 暗房 CINEMA — 前端大作业项目结构说明

> 本文档供小组全体成员查阅，便于分工协作与后续修改。

---

## 一、项目概述

本项目是一个纯前端（HTML + CSS + JS）的电影主题网站，品牌名为 **暗房 CINEMA**。网站共包含 **5 个页面**，涵盖首页展示、电影榜单、演员介绍、趣味小功能和经典电影详情等模块。

### 技术栈
- **HTML5** — 页面结构
- **CSS3** — 样式与响应式布局（无框架）
- **Vanilla JavaScript** — 交互逻辑（无框架/无库）
- **JSON** — 本地静态数据（豆瓣 Top250）

---

## 二、目录结构总览

```
front_end_final/
├── README.md                      # 原始项目简介（较简略）
├── 项目结构说明.md                # 本文档
│
├── css/
│   ├── style.css                  # 【通用样式】首页/榜单/演员/测试页共用
│   └── detail-classic.css         # 【详情页专属】暗房风格，仅 detail-classic.html 引用
│
├── js/
│   └── script.js                  # 【通用脚本】全站交互逻辑（约 1150 行）
│
├── json/
│   └── douban_top250.json         # 【数据源】豆瓣 Top250 电影原始数据
│
├── pages/                         # 【页面目录】所有 HTML 页面
│   ├── index.html                 # 首页
│   ├── movie-rankings.html        # 电影榜单 + 筛选小工具
│   ├── actors.html                # 喜爱的演员
│   ├── test.html                  # 趣味小功能（4 个互动板块）
│   └── detail-classic.html        # 经典电影详情页（5 部电影共用）
│
└── images/                        # 【静态资源】
    ├── backgrounds/
    │   └── background.jpg         # 全站背景图（除 detail-classic 外）
    ├── developer/                 # 小组成员 GitHub 头像（圆形展示在首页）
    │   ├── Devonaokii.png
    │   ├── Urie.jpg
    │   ├── WuLoong666.jpg
    │   └── lyt8396.png
    ├── carousel1~3.jpg            # 首页轮播图（3张）
    ├── Douban_Top250_Covers/      # 豆瓣 Top250 电影海报（001~250）
    ├── actors/                    # 演员相关图片
    └── 电影截图/                  # 5 部经典电影的素材
        ├── 星际穿越/
        ├── 戏梦巴黎/
        ├── 美国精神病人/
        ├── 奥兰多/
        └── 末路狂花/
```

---

## 三、页面详解

### 1. index.html — 首页
**引入样式：** `../css/style.css`
**引入脚本：** `../js/script.js` + 底部内联脚本（登录验证）

**包含模块（从上到下）：**
| 模块 | 说明 | 关键类名 / ID |
|------|------|---------------|
| 导航栏 | 固定顶部，毛玻璃效果，含品牌 Logo + 4 个页面链接 | `#detailNav`、`.site-logo`、`.nav-links` |
| 登录板块 | 用户名 / 电话 / 邮箱 表单，前端验证，不存储数据 | `.login-section`、`#loginForm` |
| 轮播图 | 3 张全屏背景图自动轮播，支持手动切换与指示器 | `.carousel`、`.carousel-slide` |
| 关于我们 | 团队名称 + 描述 + 4 人 GitHub 头像（圆形） | `.team-section`、`.member-avatar` |
| 经典电影 | 5 部经典电影卡片，点击跳转详情页 | `.classic-movies-section`、`.movies-container` |
| 页脚 | 左侧版权 + 右侧邮箱，左右分栏 | `footer`、`.footer-content` |

**注意事项：**
- 登录表单的验证逻辑以**内联 `<script>`** 写在 `index.html` 底部（`id="loginForm"` 的 submit 事件），不依赖 `script.js`。
- 经典电影卡片是从原 `classic-movies.html` 迁移过来的，保留了 `onclick="location.href='detail-classic.html?id=xxx'"` 的跳转方式，5 部电影的 ID 分别为：`interstellar`、`dreamers`、`americanpsycho`、`orlando`、`thelma`。
- 轮播图由 `script.js` 中的 `initCarousel()` 驱动，自动播放间隔 4 秒。

---

### 2. movie-rankings.html — 电影榜单
**引入样式：** `../css/style.css`
**引入脚本：** `../js/script.js`

**包含模块：**
| 模块 | 说明 | 关键类名 / ID |
|------|------|---------------|
| 导航栏 | 与首页统一 | `#detailNav` |
| 搜索区 | 输入框 + 搜索/清除按钮 + 结果计数 | `#movie-search`、`.search-btn`、`#clear-search` |
| 筛选区 | 类型/评分/年份/国家/评分人数/排名 多维度筛选 | `.filter-section`、`.filter-btn` |
| 筛选组合 | 单选按钮：满足所有条件(AND) / 满足任一(OR) | `input[name="combination"]` |
| 筛选管理 | 保存/加载/重置筛选条件 | `#save-filters`、`#load-filters`、`#reset-filters` |
| 榜单列表 | 动态生成的电影排行卡片 | `#rankings-list` |
| 分页控件 | 上一页/下一页 + 页码按钮 | `#pagination`、`.pagination-numbers` |
| 页脚 | 统一页脚 | `footer` |

**注意事项：**
- 榜单数据来自 `../json/douban_top250.json`，由 `script.js` 中的 `initMovieData()` 和 `initMovieRankings()` 处理。
- 搜索支持中文拼音首字母匹配（如输入 "dhxy" 可匹配 "大话西游"），映射表硬编码在 `script.js` 中。
- 筛选按钮通过 `data-filter` 和 `data-value` 属性标识，JS 中维护当前激活的筛选状态对象。
- 分页每页默认 10 条，页码动态生成。

---

### 3. actors.html — 喜爱的演员
**引入样式：** `../css/style.css`
**引入脚本：** `../js/script.js`

**包含模块：**
| 模块 | 说明 | 关键类名 / ID |
|------|------|---------------|
| 导航栏 | 与首页统一 | `#detailNav` |
| 演员档案 | 2 位演员（周也、汤姆·克鲁斯） | `.actors-section`、`.actor-profile` |
| 演员信息 | 头像 + 姓名/国籍/出生日期/职业 +  biography | `.actor-info`、`.actor-photo`、`.actor-bio` |
| 代表作时间线 | 纵向时间线展示 3 部代表作 | `.timeline`、`.timeline-item` |
| 剧照轮播 | 每个演员独立的 banner 剧照展示 | `#banner`、`.stage-photo` |
| 参演电影网格 | 分页展示参演电影（每页 4 部） | `.movie-grid`、`.movie-page` |
| 分页控件 | 左右箭头 + 页码信息（如 1/3） | `.movie-pagination`、`.page-btn` |
| 获奖荣誉 | 近期奖项列表 | `.awards-list`、`.award-item` |
| 页脚 | 统一页脚 | `footer` |

**注意事项：**
- 演员 biography 文本区域有 "展开/收起" 功能，由 `script.js` 中对应逻辑控制。
- 两位演员的剧照轮播和电影分页是独立的实例，通过不同 ID（`stage-photo1`、`stage-photo2`）区分。
- 参演电影数据为硬编码 HTML，非动态加载。

---

### 4. test.html — 趣味小功能
**引入样式：** `../css/style.css`
**引入脚本：** `../js/script.js`

**包含模块（4 个互动卡片）：**
| 功能 | 说明 | 关键 ID |
|------|------|---------|
| 电影主角性格小测试 | 5 道选择题，4 种结果（探险家/浪漫主义者/反派/哲人） | `#questionText`、`#answerBtns`、`#testResult`、`#restartBtn` |
| 电影冷知识每日一推 | 随机展示一条电影冷知识 | `#factContent` |
| 电影随机抽签选片器 | 从豆瓣 Top250 中随机抽取一部电影 | `#randomResult` |
| 留言投稿推荐电影 | 昵称 + 电影名 + 推荐理由表单，本地存储展示 | `#messageForm`、`#messageList` |

**注意事项：**
- 这 4 个功能全部在 `script.js` 的**第二个 `DOMContentLoaded` 监听器**中初始化（约第 884 行之后）。
- 性格测试题库和冷知识数组均硬编码在 `script.js` 中。
- 留言板使用 `localStorage` 持久化，键名为 `movieMessages`。
- 随机抽签从 `douban_top250.json` 数据中随机选取。

---

### 5. detail-classic.html — 经典电影详情页
**引入样式：** `../css/detail-classic.css`（⚠️ 注意：不是 style.css）
**引入脚本：** 底部内联 `<script>`（无外部 JS）

**设计模式：** 单页多主题。通过 URL 参数 `?id=xxx` 切换 5 部电影的展示内容。

**支持的 5 个 ID：**
| ID | 影片 | 特色主题 |
|----|------|----------|
| `interstellar` | 星际穿越 | NASA HUD 风格 + 科幻数据面板 |
| `dreamers` | 戏梦巴黎 | 巴黎拼贴画 + 旋转照片散落 |
| `americanpsycho` | 美国精神病人 | 故障艺术条带 + 滚动血液遮罩 |
| `orlando` | 奥兰多 | 古典树影 + 悬浮人物 |
| `thelma` | 末路狂花 | 公路片风格 + 双女主人物 |

**页面结构：**
| 模块 | 说明 | 关键类名 / ID |
|------|------|---------------|
| 导航栏 | Logo + 分页切换 + 页面链接 | `#detailNav`、`#pagination` |
| Hero 大图区 | 全屏背景 + 人物剪影 + 影片信息 | `#detailHero`（动态生成 class） |
| 第二幕 | 6 宫格卡片：海报/剧照/影评混排 | `.second-act`、`.cards-grid` |
| 灯箱 | 点击图片/影评卡片弹出全屏查看 | `#lightbox`、`.lightbox-content` |
| 页脚 | 统一页脚 | `footer` |

**注意事项（极其重要）：**
- 本页面**不使用** `style.css`，而是使用独立的 `detail-classic.css`，因此导航栏和页脚的样式在 detail-classic.css 中有一份独立定义（与 style.css 保持视觉统一）。
- 所有内容（Hero、第二幕、灯箱、分页）均由底部**内联 IIFE 脚本**动态生成，根据 `?id=` 参数决定渲染哪部电影。
- 5 部电影的 Hero 布局和第二幕结构各不相同，代码中以 `if/else if` 分支处理。
- `americanpsycho` 分支包含一个独立的滚动监听 IIFE，实现血液图片的 `clip-path` 遮罩动画。
- 灯箱点击背景或 × 按钮可关闭，关闭后恢复页面滚动。
- 分页区域 (`#pagination`) 在导航栏内部，显示 "← Prev / Next →" 切换上下一部影片。

---

## 四、CSS 架构说明

### 4.1 文件分工

| CSS 文件 | 被引用的页面 | 职责 |
|----------|-------------|------|
| `css/style.css` | index, movie-rankings, actors, test | 通用暗房风格：导航、字体、背景渐变、登录/轮播/卡片/页脚等 |
| `css/detail-classic.css` | detail-classic | 详情页专属：5 套 Hero 主题、第二幕网格、灯箱、特殊动画（血液遮罩等） |

### 4.2 关键样式约定

- **字体：** 全站使用 Google Fonts `Noto Serif SC` + `Playfair Display` + `Oranienbaum`，在 style.css 和 detail-classic.css 中均通过 `@import` 引入。
- **背景：** style.css 页面使用 `linear-gradient` 黑色遮罩 + `background.jpg`；detail-classic 使用纯黑/噪点背景。
- **导航栏统一：** 所有页面均使用 `<nav id="detailNav">`，包含 `.site-logo` + `.nav-links` 结构。`.site-logo` 颜色统一为 `#e50914`。
- **页脚统一：** 所有页面均使用 `<footer><div class="footer-content">...</div></footer>`，flex 布局，`gap: 2rem`。
- **响应式：** 两处 CSS 均包含 `@media` 查询，主要断点在 768px 和 480px 左右。

### 4.3 添加新页面时的 CSS 建议

如果新增普通内容页（非详情页），直接引用 `style.css` 即可，并复制以下导航/页脚 HTML 结构：

```html
<nav id="detailNav">
  <a href="index.html" class="site-logo">暗房CINEMA</a>
  <ul class="nav-links">
    <li><a href="index.html">首页</a></li>
    <li><a href="actors.html">喜爱的演员</a></li>
    <li><a href="movie-rankings.html">电影榜单</a></li>
    <li><a href="test.html">趣味小功能</a></li>
  </ul>
</nav>

<footer>
  <div class="footer-content">
    <span>© 2026 暗房CINEMA 版权所有</span>
    <span>联系邮箱：2782938791@qq.com</span>
  </div>
</footer>
```

---

## 五、JavaScript 架构说明

### 5.1 文件：`js/script.js`

所有通用交互集中在此文件，按功能划分为以下模块：

| 函数/模块 | 作用范围 | 说明 |
|-----------|---------|------|
| `initCarousel()` | index.html | 轮播图：自动播放、prev/next、指示器点击 |
| `initExpandButtons()` | index.html（原） | 展开/收起按钮（经典电影描述），HTML 中按钮已移除但 CSS/JS 保留 |
| `initMovieData()` | movie-rankings, test | 加载 `douban_top250.json`，处理数据并暴露接口 |
| `getPinyinInitial()` | movie-rankings | 中文转拼音首字母，用于搜索匹配 |
| `initMovieRankings()` | movie-rankings | 搜索 + 多维度筛选 + 分页 + 高亮 + 筛选保存加载 |
| `initactors()` | actors.html | 演员页轮播图 (`initCarousel2`) |
| 第二 DOMContentLoaded | test.html | 性格测试、冷知识、随机抽签、留言板 |

### 5.2 执行时机

- `script.js` 在文件末尾统一监听 `DOMContentLoaded`，按顺序调用上述初始化函数。
- 各函数内部会检测对应 DOM 元素是否存在，不存在则静默跳过，因此**同一 script.js 被所有页面引用不会报错**。

### 5.3 detail-classic.html 的脚本

该页面**不引用 script.js**，所有逻辑写在页面底部内联 `<script>` 中：
- `movies` 对象：5 部电影的元数据（标题、年份、导演、类型、引言、描述、海报路径）。
- `order` 数组：定义 5 部电影的循环顺序，用于分页 Prev/Next。
- `getParam()`：解析 URL 参数。
- 主 IIFE：根据 ID 渲染 Hero + 第二幕 + 灯箱 + 分页。

---

## 六、数据与图片资源规范

### 6.1 JSON 数据格式

`json/douban_top250.json` 为对象数组，每个对象包含以下字段：
```json
{
  "排名": 1,
  "电影名称": "肖申克的救赎",
  "导演": "弗兰克·德拉邦特",
  "主演": "蒂姆·罗宾斯 / 摩根·弗里曼",
  "评分": "9.7",
  "评分人数": "293",
  "引言": "希望让人自由。",
  "详情链接": "https://movie.douban.com/subject/1292052/",
  "类别": "犯罪 / 剧情",
  "上映年份": "1994",
  "国家": "美国"
}
```

**注意：** `评分人数` 字段单位是"万"，如 "293" 表示 293 万人。

### 6.2 图片命名约定

| 目录 | 用途 | 命名示例 |
|------|------|----------|
| `images/Douban_Top250_Covers/` | 豆瓣榜单海报 | `001.jpg` ~ `250.jpg`，与 JSON 中排名一一对应 |
| `images/电影截图/<影片名>/` | 详情页素材 | 各影片目录下放海报、背景、人物 PNG、剧照等 |
| `images/actors/` | 演员页素材 | `actor1.jpg`、`a1-movie1.jpg` 等，按演员和电影编号 |
| `images/developer/` | 首页头像 | 使用 GitHub 用户名命名，支持 png/jpg |

---

## 七、团队协作指南

### 7.1 修改现有页面

| 想改什么 | 应该编辑的文件 | 注意事项 |
|----------|---------------|----------|
| 修改导航栏文字/链接 | 所有 `pages/*.html` | 5 个文件都要改，保持同步 |
| 修改导航栏样式 | `css/style.css` + `css/detail-classic.css` | 两处都要改，保持视觉统一 |
| 修改页脚内容 | 所有 `pages/*.html` | 5 个文件都要改 |
| 修改页脚样式 | `css/style.css` + `css/detail-classic.css` | 同上 |
| 修改首页轮播图 | `pages/index.html`（img src）+ `images/carousel*.jpg` | 保持 3 张图尺寸一致 |
| 修改经典电影卡片 | `pages/index.html` | 卡片结构和跳转链接 |
| 修改榜单筛选条件 | `pages/movie-rankings.html` + `js/script.js` | HTML 放按钮，JS 放筛选逻辑 |
| 修改演员信息 | `pages/actors.html` | 直接编辑硬编码内容 |
| 修改冷知识/测试题 | `js/script.js` | 搜索对应数组直接修改 |

### 7.2 添加新页面

1. 在 `pages/` 下新建 `.html` 文件。
2. 引用 `../css/style.css`（如果是普通内容页）。
3. 复制统一的导航栏和页脚结构（见 4.3 节）。
4. 引用 `../js/script.js`（如有需要）。
5. 在所有其他页面的导航栏 `<ul class="nav-links">` 中添加新页面链接。

### 7.3 向 detail-classic 添加新电影

1. 在 `detail-classic.html` 的 `movies` 对象中新增条目。
2. 在 `order` 数组中加入新 ID。
3. 在 `if/else if` 链中新增一个分支，设计 Hero 和第二幕布局。
4. 在 `images/电影截图/` 下新建文件夹，放入素材。
5. 在 `css/detail-classic.css` 中新增对应的 Hero 主题样式。

**注意：** detail-classic.html 的内联脚本目前采用 `if/else if` 链，每增加一部电影就需要新增一个分支，这是当前架构的主要扩展瓶颈。若需频繁添加，建议后续将每部电影的配置提取为数据对象。

---

## 八、常见陷阱与排雷

1. **导航栏/页脚改了 style.css 但 detail-classic 没变化？**
   - detail-classic.html 引用的是 `detail-classic.css`，不是 `style.css`。两处 CSS 中各维护了一份 `#detailNav` 和 `footer` 的样式，修改时需要同步更新两个文件。

2. **搜索时拼音首字母匹配不到？**
   - `script.js` 中的 `pinyinMap` 是硬编码的，只覆盖了常见汉字。若出现匹配失败，需要在 `pinyinMap` 中补充对应汉字的拼音首字母。

3. **经典电影卡片点击跳转 404？**
   - 确保 URL 参数与 `detail-classic.html` 中 `movies` 对象的 key 完全一致（区分大小写）。

4. **新增了 filter 按钮但筛选不生效？**
   - HTML 中的 `data-filter` 和 `data-value` 必须与 `script.js` 中 `initMovieRankings()` 的处理逻辑对应。同时检查按钮是否包含 `filter-btn` 类名。

5. **本地直接打开 HTML 文件时 JSON 加载失败？**
   - 由于使用了 `fetch()` 加载 `douban_top250.json`，若直接以 `file://` 协议打开页面，部分浏览器会拦截跨域请求。建议通过本地服务器（如 VS Code Live Server）访问。

6. ** actors.html 的剧照不显示？**
   - 剧照路径在 `js/script.js` 的 `initactors()` 中设置，请检查对应图片是否存在于 `images/actors/` 目录。

---

## 九、版本与维护记录

| 日期 | 修改人 | 内容摘要 |
|------|--------|----------|
| 2026-05 | Urie / 小克 | 项目结构重构：统一导航栏/页脚、合并 classic-movies 到首页、提取 detail-classic 公共代码、编写本文档 |

> 后续修改请在上方追加记录，便于追溯。
